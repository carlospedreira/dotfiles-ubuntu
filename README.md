## Install

```bash
cd ~
sh -c "$(curl -fsLS https://get.chezmoi.io/lb)" -- init --apply carlospedreira/dotfiles-ubuntu
```

Run this from your home directory (`~`), not from a Windows mount like `/mnt/c/...`, to avoid permission errors during installation.
